on: [push]
jobs:
  build:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        configuration: [Release, Debug]
    outputs:
      hash-Release: ${{ steps.hash.outputs.hash-Release }}
      hash-Debug: ${{ steps.hash.outputs.hash-Debug }}
    steps:
      - uses: actions/checkout@v4
      - uses: microsoft/setup-msbuild@v2
      - name: vcpkg build
        id: vcpkg
        uses: johnwason/vcpkg-action@v6
        with:
          manifest-dir: ${{ github.workspace }} # Set to directory containing vcpkg.json
#         pkgs: nlohmann-json boost-asio boost-optional boost-utility boost-date-time boost-beast wintoast openssl
          triplet: x64-windows-static
          token: ${{ github.token }}
          github-binarycache: true
      - run: vcpkg integrate install
      - run: msbuild LGTVCompanion.sln /p:VcpkgEnableManifest=true 
        env:
          Configuration: ${{ matrix.configuration }}
      - name: Generate hash
        id: hash
        run: |
          echo "hash-${{ matrix.configuration }}=$( sha256sum 'LGTV Companion Setup/bin/${{ matrix.configuration }}/LGTV Companion Setup.msi' | base64 -w0 )" >> "$GITHUB_OUTPUT"
          echo "hash-${{ matrix.configuration }}=$( sha256sum 'LGTV Companion Setup/bin/${{ matrix.configuration }}/LGTV Companion Setup.msi' | base64 -w0 )"
          sha256sum 'LGTV Companion Setup/bin/${{ matrix.configuration }}/LGTV Companion Setup.msi'
      - uses: actions/upload-artifact@v4
        with:
          name: LGTVCompanion-Output-${{ matrix.configuration }}
          path: x64/${{ matrix.configuration }}/
      - uses: actions/upload-artifact@v4
        with:
          name: LGTVCompanion-Setup-${{ matrix.configuration }}
          path: LGTV Companion Setup/bin/${{ matrix.configuration }}/  
  testing:
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - name: Test passing output
        id: output-test
        run: echo "${{ needs.build.outputs.hash-Release }}"
  provenance:
    needs: [build]
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      actions: read # To read the workflow path.
      id-token: write # To sign the provenance.
      contents: write # To add assets to a release.
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v2.0.0
    with:
      base64-subjects: "${{ needs.build.outputs.hash-Release }}"
      draft-release: true
      upload-assets: true # Optional: Upload to a new release
  release:
    needs: [build, provenance]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write # To add assets to a release.
    steps:
      - name: Download Installer
        uses: actions/download-artifact@v4
        with:
          name: LGTVCompanion-Setup-Release
      - name: Upload assets
        uses: softprops/action-gh-release@v2
        with:
          files: LGTV Companion Setup.msi
          draft: true
          generate_release_notes: true
